# Use PostgreSQL

The challenge allows either MySQL or PostgreSQL. We decided to use PostgreSQL because it is a strong default for Laravel applications with relational constraints, gives comfortable room for enforcing registration uniqueness rules, and remains easy to run locally with common tooling such as Laravel Herd and TablePlus.
